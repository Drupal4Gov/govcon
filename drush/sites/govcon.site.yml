local:
    root: /var/www/govcon/docroot
    uri: 'http://local.govcon.com'
    host: local.govcon.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
